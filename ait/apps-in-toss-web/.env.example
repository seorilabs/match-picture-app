# 앱인토스 통합 광고(Toss Ads + AdMob) 광고 그룹 ID.
# AIT 콘솔 > 수익화 > 인앱 광고에서 발급받은 광고 그룹 ID를 넣는다.
# 미설정(빈 값)이면 광고는 자동으로 비활성화된다(supported()=false → no-op).
#
# 이 게임은 전면형 하나만 쓴다. 원본 Unity 도 RETRY 버튼에서만 전면광고를 띄웠다.
# 개발/테스트용 ID(실 광고 ID로 테스트 금지): ait-ad-test-interstitial-id
VITE_TOSS_INTERSTITIAL_AD_GROUP_ID=
